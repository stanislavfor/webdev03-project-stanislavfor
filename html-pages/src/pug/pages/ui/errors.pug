extends ../../layouts/ui-layout.pug

block variables
	- var title = 'Элементы - UI-kit и HTML фреймворк - Документация'
	- var pageClass = 'body--dashboard'

block content
	include ../../sections/ui-nav-inside-ui.pug

	.main-wrapper
		.container.container--left
			.title.mb-0 Ошибки
			p Нотификации. Занимают всю ширину контейнера	
			p Однострочный контейнер с ошибкой

			.section-ui
				+error('Комментарий не может быть пустым.')
			pre.
				+error('Комментарий не может быть пустым.')

			.section-ui
			p Другой образец ошибки
			.section-ui
				+error-with-desc('Данный email уже занят')
					p Используйте другой email, чтобы создать новый аккаунт
					p или воспользуйтесь #[a(href="#" target="_blank") восстановлением пароля] , чтобы войти на сайт.
			pre.
				+error-with-desc('Данный email уже занят')
					p Используйте другой email, чтобы создать новый аккаунт
					p или воспользуйтесь #[a(href="#" target="_blank") восстановлением пароля] , чтобы войти на сайт.
